# ⚙️ 0.2 – Install & Setup VS Code

## 🧠 What is VS Code?
**VS Code (Visual Studio Code)** is a free code editor made by Microsoft.  
We use it to **write, run, and manage** our JavaScript code easily.

---

## 💡 Why We Use VS Code
- It supports **JavaScript by default**.  
- Has a **built-in terminal** (you can run Node.js inside it).  
- You can install **extensions** to make coding faster and easier.  
- It’s **lightweight**, **fast**, and works on **Windows, Mac, and Linux**.  

---

## 🪄 Step 1 – Download & Install

1. Go to 👉 [https://code.visualstudio.com](https://code.visualstudio.com)  
2. Download VS Code for your system (Windows/macOS/Linux).  
3. Open the downloaded file and install it with default settings.  
4. After install → open **VS Code**.  
5. You should see a clean window with a welcome screen.

---

## 🧩 Step 2 – Install Useful Extensions

Click on the **Extensions icon (four blocks)** on the left sidebar.  
Search and install these:

| Extension | What It Does |
|------------|--------------|
| **Prettier – Code Formatter** | Automatically makes your code look clean |
| **Live Server** | Lets you run HTML + JS files live in browser |
| **Auto Rename Tag** | Auto changes closing tag when you edit opening tag |
| **Path Intellisense** | Helps with file path suggestions |
| **Code Spell Checker** | Fixes small spelling mistakes |
| **Error Lens** | Highlights any error instantly |
| **JavaScript (ES6) code snippets** | Gives ready JS templates for faster coding |

✅ These will make your workflow smoother.

---

## 🎨 Step 3 – Basic Settings Setup

Press **Ctrl + ,** (comma) → search for “Settings (JSON)”  
or  
Go to `File → Preferences → Settings → Open Settings (JSON)`  
Then paste this:

```json
{
  "editor.fontSize": 16,
  "editor.formatOnSave": true,
  "editor.wordWrap": "on",
  "files.autoSave": "afterDelay",
  "workbench.colorTheme": "Default Dark+"
}
```
- 👉 This will:
    - Make text bigger
    - Auto-format on save
    - Wrap long lines
    - Auto-save after a few seconds
    - Apply a dark theme
---
### Step 4 – Test Your Setup
1.Open VS Code

2.Create a new file named test.js

3.Add this code:

```javascript
    console.log("Hello This is Vamshi 👋");
```

4. Open the terminal (Ctrl + `)

5. Run this command:

```bash
    node test.js
```

6. If you see this output:

```css
    Hello This is Vamshi 👋
```
-> Your setup is working perfectly ✅